{
  "name": "web-profile",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --port 5174 --host"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}